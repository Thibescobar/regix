"""Intentional vulture exceptions.

The file is deliberately empty at 0.2.0. Keeping it explicit makes any future
public callback or plugin hook a reviewed exception instead of lowering the global
confidence threshold.
"""
